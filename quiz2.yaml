---
  - name: 메모리가 300MB 이상 되었을 때 메시지 출력
    hosts: serverd.test.com
    tasks:
      - name: example when 1
        debug:
          msg: this system has big memory
        when: ansible_facts.memfree_mb >= 300